<h1>Are you sure you want to delete @Model["stylist"].GetName()?</h1>

<form action='/stylists/@Model["stylist"].GetId()/deleted' method='post'>
    <button type='submit'>YES</button>
</form>
<form action='/stylists/@Model["stylist"].GetId()'>
    <button type='submit'>NO</button>
</form>