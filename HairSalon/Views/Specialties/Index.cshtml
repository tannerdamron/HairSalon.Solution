<h2>All specialties:</h2>

@if (@Model.Count == 0)
{
  <h3>No specialties have been added yet!</h3>
}

@if (Model.Count != 0)
{
    <ul>
    @foreach (var specialty in Model)
    {
        <li><a href='/specialties/@specialty.GetId()'>@specialty.GetName()</a></li>
    }
    </ul>
}

<h3><a href='/specialties/new'>Add a new specialty</a></h3>
<h3><p><a href='/stylists'>Go to stylist list to add new specialties to stylist</a></p></h3>
<p><a href='/'>Back to home page</a></p>